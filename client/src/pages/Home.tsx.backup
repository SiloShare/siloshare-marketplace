import { useAuth } from "@/_core/hooks/useAuth";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { useLocation } from "wouter";

export default function Home() {
  const [, setLocation] = useLocation();

  const handleCadastrarSilo = () => {
    setLocation("/cadastrar-silo");
  };

  const handleBuscarArmazenagem = () => {
    setLocation("/buscar-armazenagem");
  };

  const handleComecarAgora = () => {
    // TODO: Implementar navegação para registro
    alert("Funcionalidade em desenvolvimento: Cadastro de Usuário");
  };

  return (
    <div className="min-h-screen bg-white">
      {/* Header */}
      <header className="border-b border-gray-200 bg-white sticky top-0 z-50 shadow-sm">
        <div className="container mx-auto px-4 py-4 flex items-center justify-between">
          <div className="flex items-center gap-2">
            <span className="text-3xl">🌾</span>
            <span className="text-2xl font-bold text-black">SiloShare</span>
          </div>
          <nav className="hidden md:flex gap-6">
            <a href="#como-funciona" className="text-gray-700 hover:text-black transition">Como Funciona</a>
            <a href="#beneficios" className="text-gray-700 hover:text-black transition">Benefícios</a>
            <a href="#contato" className="text-gray-700 hover:text-black transition">Contato</a>
          </nav>
          <Button onClick={handleComecarAgora} className="bg-black hover:bg-gray-800 text-white">
            Começar Agora
          </Button>
        </div>
      </header>

      {/* Hero Section com Background */}
      <section 
        className="relative py-32 px-4 bg-cover bg-center"
        style={{
          backgroundImage: 'linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url(/silo-hero-bg.jpg)',
        }}
      >
        <div className="container mx-auto max-w-4xl text-center">
          <h1 className="text-5xl md:text-6xl font-bold text-white mb-6">
            Conectamos Armazenagem e Transporte de Grãos
          </h1>
          <p className="text-xl text-white/90 mb-8">
            O primeiro marketplace do Brasil que une donos de silos, produtores e transportadoras
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <Button 
              onClick={handleCadastrarSilo}
              size="lg" 
              className="bg-green-700 hover:bg-green-800 text-lg px-8"
            >
              Cadastrar Meu Silo
            </Button>
            <Button 
              onClick={handleBuscarArmazenagem}
              size="lg" 
              variant="outline" 
              className="border-white text-white hover:bg-white/10 text-lg px-8"
            >
              Buscar Armazenagem
            </Button>
          </div>
        </div>
      </section>

      {/* Paradoxo Section */}
      <section className="py-16 px-4 bg-white">
        <div className="container mx-auto max-w-6xl">
          <h2 className="text-4xl font-bold text-center mb-4">O Paradoxo da Armazenagem no Brasil</h2>
          <p className="text-center text-gray-600 mb-12">
            Enquanto falta armazenagem para milhões de toneladas, silos ficam ociosos metade do ano
          </p>
          
          <div className="grid md:grid-cols-3 gap-8">
            <Card className="border-red-200 bg-red-50">
              <CardHeader>
                <CardTitle className="text-5xl font-bold text-red-700">95,2M</CardTitle>
                <CardDescription className="text-lg font-semibold text-gray-700">
                  Toneladas sem Armazenagem
                </CardDescription>
              </CardHeader>
              <CardContent>
                <p className="text-gray-600">
                  Déficit de 29,5% na capacidade de armazenagem no Brasil
                </p>
              </CardContent>
            </Card>

            <Card className="border-orange-200 bg-orange-50">
              <CardHeader>
                <CardTitle className="text-5xl font-bold text-orange-700">40-50%</CardTitle>
                <CardDescription className="text-lg font-semibold text-gray-700">
                  Silos Ociosos
                </CardDescription>
              </CardHeader>
              <CardContent>
                <p className="text-gray-600">
                  Dos silos ficam vazios 5-6 meses por ano no Centro-Oeste
                </p>
              </CardContent>
            </Card>

            <Card className="border-yellow-200 bg-yellow-50">
              <CardHeader>
                <CardTitle className="text-5xl font-bold text-yellow-700">15-30%</CardTitle>
                <CardDescription className="text-lg font-semibold text-gray-700">
                  Custo de Transporte
                </CardDescription>
              </CardHeader>
              <CardContent>
                <p className="text-gray-600">
                  Do valor da produção é perdido em logística ineficiente
                </p>
              </CardContent>
            </Card>
          </div>
        </div>
      </section>

      {/* Como Funciona Section */}
      <section id="como-funciona" className="py-16 px-4">
        <div className="container mx-auto max-w-6xl">
          <h2 className="text-4xl font-bold text-center mb-4">Como Funciona</h2>
          <p className="text-center text-gray-600 mb-12">Simples, rápido e seguro em 6 passos</p>
          
          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            {[
              { icon: "👤", titulo: "Cadastre-se", descricao: "Crie sua conta como dono de silo, produtor ou transportadora" },
              { icon: "🔍", titulo: "Busque ou Anuncie", descricao: "Produtores buscam silos. Donos cadastram capacidade disponível" },
              { icon: "🚚", titulo: "Cotação de Transporte", descricao: "Sistema envia cotação automática para transportadoras" },
              { icon: "📊", titulo: "Compare Propostas", descricao: "Receba e compare ofertas de armazenagem e transporte" },
              { icon: "✅", titulo: "Contrate", descricao: "Escolha a melhor opção e feche o negócio na plataforma" },
              { icon: "📱", titulo: "Monitore", descricao: "Acompanhe reservas, transporte e pagamentos em tempo real" },
            ].map((passo, index) => (
              <Card key={index} className="hover:shadow-lg transition">
                <CardHeader>
                  <div className="text-5xl mb-2 grayscale">{passo.icon}</div>
                  <CardTitle className="text-sm text-green-700 font-semibold">PASSO {index + 1}</CardTitle>
                  <CardDescription className="text-xl font-bold text-gray-900">
                    {passo.titulo}
                  </CardDescription>
                </CardHeader>
                <CardContent>
                  <p className="text-gray-600">{passo.descricao}</p>
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
      </section>

      {/* Benefícios Section */}
      <section id="beneficios" className="py-16 px-4 bg-white">
        <div className="container mx-auto max-w-6xl">
          <h2 className="text-4xl font-bold text-center mb-12">Por Que Escolher o SiloShare?</h2>
          
          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            {[
              { icon: "📍", titulo: "Geolocalização", descricao: "Encontre silos próximos à sua propriedade" },
              { icon: "⚡", titulo: "Economia de Tempo", descricao: "Cotação automática em minutos, não dias" },
              { icon: "🔒", titulo: "Segurança", descricao: "Contratos digitais e pagamentos protegidos" },
              { icon: "🚛", titulo: "Transporte Integrado", descricao: "Armazenagem + transporte em um só lugar" },
              { icon: "⭐", titulo: "Rede Confiável", descricao: "Usuários verificados e avaliações reais" },
              { icon: "💰", titulo: "Transparência", descricao: "Compare preços e condições facilmente" },
            ].map((beneficio, index) => (
              <Card key={index} className="text-center hover:shadow-lg transition">
                <CardHeader>
                  <div className="text-5xl mb-2 grayscale">{beneficio.icon}</div>
                  <CardTitle className="text-xl">{beneficio.titulo}</CardTitle>
                </CardHeader>
                <CardContent>
                  <p className="text-gray-600">{beneficio.descricao}</p>
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
      </section>

      {/* CTA Final Section */}
      <section className="py-20 px-4 bg-gradient-to-r from-green-700 to-green-900 text-white">
        <div className="container mx-auto max-w-4xl text-center">
          <h2 className="text-4xl font-bold mb-4">Faça Parte da Revolução da Armazenagem</h2>
          <p className="text-xl mb-8 opacity-90">
            Cadastre-se agora e seja um dos primeiros a usar o SiloShare. Vagas limitadas para early adopters!
          </p>
          <Button 
            onClick={handleComecarAgora}
            size="lg" 
            className="bg-white text-green-700 hover:bg-gray-100 text-lg px-12"
          >
            Quero Participar
          </Button>
          <p className="mt-4 text-sm opacity-75">
            Ao se cadastrar, você receberá acesso antecipado e condições especiais.
          </p>
        </div>
      </section>

      {/* Footer */}
      <footer id="contato" className="py-12 px-4 bg-gray-900 text-white">
        <div className="container mx-auto max-w-6xl">
          <div className="grid md:grid-cols-3 gap-8">
            <div>
              <div className="flex items-center gap-2 mb-4">
                <span className="text-3xl">🌾</span>
                <span className="text-2xl font-bold">SiloShare</span>
              </div>
              <p className="text-gray-400">
                Conectando o agronegócio brasileiro através da tecnologia.
              </p>
            </div>
            
            <div>
              <h3 className="font-bold mb-4">Links Rápidos</h3>
              <ul className="space-y-2 text-gray-400">
                <li><a href="#como-funciona" className="hover:text-white transition">Como Funciona</a></li>
                <li><a href="#beneficios" className="hover:text-white transition">Benefícios</a></li>
                <li><a href="#contato" className="hover:text-white transition">Contato</a></li>
              </ul>
            </div>
            
            <div>
              <h3 className="font-bold mb-4">Contato</h3>
              <ul className="space-y-2 text-gray-400">
                <li>📧 contato@siloshare.com.br</li>
                <li>📱 (65) 99999-9999</li>
                <li>📍 Sorriso, MT - Brasil</li>
              </ul>
            </div>
          </div>
          
          <div className="border-t border-gray-800 mt-8 pt-8 text-center text-gray-400">
            <p>&copy; 2025 SiloShare. Todos os direitos reservados.</p>
          </div>
        </div>
      </footer>
    </div>
  );
}

